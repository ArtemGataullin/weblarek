# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` — принимает базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - выполняет POST запрос с данными.
`handleResponse(response: Response): Promise<object>` - защищенный метод проверки ответа сервера.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

##### Данные
В проекте были использованны интерфейсы Товара и покупателя:
Интерфейс `IProduct`: 
Определяет структуру данных карточки товара. Служит для вывода информации о продукте, предлагаемом к продаже. Содержит полный набор атрибутов, требуемых для распознавания позиции, а также для информирования пользователя об её свойствах, внешнем виде и цене. Данный интерфейс состоит из:
1. `id: string` - уникальный идентификатор товара;
2. `title: string` - имя товара используемое при отображении в карточках, катологе и в корзине;
3. `image: string` - путь к изображению товара используемое при отображении в карточках и в катологе;
4. `category: string` - Категории товара к которй он принадлежит;
5. `price: number | null` - Цена товара, значение `null` нужно когда цена товара отсутствует;
6. `description: string` -   Описания товара, с подробными характеристиками товара;

Интерфейс `IBuyer`: 
Задаёт формат хранения информации о пользователе приложения. Включает набор данных, необходимых для совершения покупки: платёжный метод, контактную информацию и адрес отгрузки. Данная структура обеспечивает корректную обработку заказов и возможность связи с клиентом. 
Данный интерфейс состоит из:
1. `payment: TPayment` -  Способ оплаты товара. В нем есть ограничения, `type TPayment = 'card' | 'cash' | ''` - содержит набор определенных способов отплаты. Гарантирует, что поле payment может принимать только одно из указанных значений.;
2. `address: string` - адресс покупателя, указывается при оформлении товара;
3. `email: string` -  электронная почта покупателя, указывается при оформлении товара;
4. `phone: string` -  номер телефона покупателя, указывается при оформлении товара;

##### Модели данных

Класс `ProductCatalog`: Обеспечивает управление списком доступных продуктов и фиксирует элемент, выбранный пользователем для подробного ознакомления. Предлагает набор фундаментальных операций для взаимодействия с каталогом

Конструктор: `constructor(protected events: IEvents)` — инициализирует пустой массив для хранения товаров и переменную для выбранного товара (без аргументов).

Поля класса:

1. `protected products: IProduct[]` — массив всех товаров в каталоге;
2. `protected selectedProduct: IProduct | null` — товар, выбранный для полного отображения.

Методы:

1. `saveProducts(products: IProduct[]): void` — сохраняет переданный массив товаров в каталог;
2. `getProducts(): IProduct[]` — возвращает полный список товаров из каталога;
3. `getProductByID(id: string): IProduct | undefined` — ищет и возвращает товар по его идентификатору;
4. `setSelectedProduct(product: IProduct | null): void` — сохраняет товар для подробного просмотра;
5. `getSelectedProduct(): IProduct | null` — возвращает выбранный для отображения товар.

Класс `ShoppingCart`: Содержит список позиций, выбранных пользователем для покупки. Предоставляет все необходимые методы для управления корзиной: получение состава, добавление и удаление товаров, очистку, вычисление общей стоимости и количества единиц, а также проверку присутствия товара в корзине.

Конструктор: `constructor(protected events: IEvents)` — создаёт пустой массив для хранения выбранных товаров (без аргументов).

Поле класса:

1. `protected selectedProducts: IProduct[]` — список всех товаров в корзине покупателя.

Методы:

1. `getSelectedProducts(): IProduct[]` — возвращает все товары, находящиеся в корзине;
2. `addSelectedProduct(product: IProduct)`: void — добавляет указанный товар в корзину;
3. `deleteSelectedProduct(id: string)`: void — удаляет товар с заданным id из корзины (через фильтрацию);
4. `clearShoppingCart(): void` — очищает корзину, устанавливая пустой массив;
5. `getTotal(): number` — вычисляет и возвращает общую стоимость товаров в корзине (0 для пустой корзины или при отсутствии цен);
6. `getSelectedProductsAmount(): number` — возвращает количество позиций в корзине через свойство `length` массива;
7. `checkSelectedProduct(id: string): boolean` — проверяет наличие товара с указанным id в корзине: `true` — найден, `false` — отсутствует.

Класс `Buyer`: Хранит информацию о покупателе и позволяет ею управлять: сохранять, считывать, очищать и проверять правильность заполнения — с точным указанием, какое именно поле заполнено неверно..

Конструктор: `constructor(protected events: IEvents)` — инициализирует объект покупателя с пустыми значениями всех полей.

Поля класса:

1. `protected data: IBuyer` — объект с данными покупателя.

Методы:

1. `savePaymentType(payment: TPayment): void` — обновляет способ оплаты в данных покупателя без изменения других полей;
2. `saveAddress(address: string): void` — сохраняет адрес доставки, не затрагивая остальные данные;
3. `saveEmail(email: string): void` — записывает email клиента, оставляя остальные поля без изменений;
4. `savePhone(phone: string): void` — фиксирует номер телефона, не меняя другие сведения;
5. `getData(): IBuyer` — возвращает полный объект с данными покупателя;
6. `clearBuyerData(): void` — сбрасывает все поля к пустым значениям, очищая информацию о клиенте;
7. `validate(): {TBuyerValidationErrors}` — проверяет корректность всех полей. Для каждого возвращает пустую строку (если валидно) или текст ошибки (если невалидно). Поле считается валидным, если после обрезки пробелов оно не пустое.

#####  Слой коммуникации

Класс `ServerApi` 
Отвечает за обмен данными с сервером. Использует композицию с классом Api.

Конструктор: `constructor(api: IApi)` — принимает экземпляр класса Api.

Методы:

`getProducts(): Promise<IProductListApi>` — получает список товаров.
`postOrder(orderRequest: IOrderRequest): Promise<TOrderResponse>` — отправляет заказ.

###### Представления (Views)
Класс `Card` и его наследники
`Card` — базовый класс для всех карточек. Содержит общие сеттеры для `title` и `price`.

`CardWithImage` — промежуточный класс, наследующий Card. Добавляет сеттеры для `image` и `category`. Используется для карточек, у которых есть изображение и категория.

`CardCatalog` — карточка для галереи. Наследуется от `CardWithImage`. При клике вызывает переданный колбэк.

`CardPreview` — карточка для модального окна. Наследуется от `CardWithImage`. Содержит кнопку для добавления/удаления из корзины.

`CardBasket` — карточка для корзины. Наследуется от `Card` (без изображения и категории). Отображает порядковый номер и кнопку удаления.

Класс `Basket`
Отвечает за отображение корзины с товарами.

Конструктор: `constructor(container: HTMLElement, events: EventEmitter)` - принимает контейнер для корзины и брокер событий.

Сеттеры:

`list(items: HTMLElement[]): void` — обновляет список товаров.

`price(value: number): void` — обновляет общую стоимость.

Класс `Form` и его наследники
Form - базовый класс для всех форм. Обрабатывает ввод данных и управляет состоянием кнопки отправки.

`OrderForm` - форма для ввода данных заказа (способ оплаты и адрес). Содержит кнопки выбора способа оплаты и поле для ввода адреса. Генерирует события при изменении полей.
`ContactsForm` - форма для ввода контактных данных (email и телефон). Генерирует события при изменении полей.

Класс `Modal`
Отвечает за отображение модальных окон.

Методы:

`open(): void` — открывает модальное окно.
`close(): void` — закрывает модальное окно.
`render(data?: IModal): HTMLElement` — рендерит содержимое и открывает окно.

Класс `Header`
Отвечает за отображение шапки сайта с счетчиком товаров в корзине.

Класс `Success`
Отображает сообщение об успешном оформлении заказа.


###### Взаимодействие компонентов

События изменения данных

1. `items:changed`	Изменение списка товаров	`ProductCatalog.saveProducts()`
2. `basket:changed`	Изменение состояния корзины	`ShoppingCart.addSelectedProduct(), deleteSelectedProduct(), clearShoppingCart()`
3. `card:selected`	Выбор карточки для превью	`ProductCatalog.setSelectedProduct()`
4. `buyer:changed`	Изменение данных покупателя	`Buyer.save*(), clearBuyerData()`
5. `modal:open / modal:close`	Открытие/закрытие модального окна	`Modal.open(), Modal.close()`

События взаимодействия пользователя
1. `card:open`	Открытие превью товара	Клик по карточке в галерее
2. `basket:open`	Открытие корзины	Клик по кнопке корзины
3. `basket:submit`	Оформление заказа	Клик по кнопке "Оформить"
4. `preview:addToBasket`	Добавление/удаление из превью	Клик по кнопке в CardPreview
5. `basket:removeItem`	Удаление из корзины	Клик по кнопке в CardBasket
6. `order.address:changed`	Изменение адреса	Ввод в поле формы
7. `order.payment:changed`	Выбор способа оплаты	Клик по кнопке оплаты
8. `order:submit`	Подтверждение заказа	Клик по кнопке "Далее"
9. `contacts.email:changed`	Изменение email	Ввод в поле формы
10. `contacts.phone:changed`	Изменение телефона	Ввод в поле формы
11. `contacts:submit`	Подтверждение оплаты	Клик по кнопке "Оплатить"
12. `success:close`	Закрытие сообщения об успехе	Клик по кнопке в Success

Обработчики событий в main.ts
1. `items:changed` — обновляет галерею и счетчик.
2. `basket:changed` — обновляет счетчик и список корзины.
3. `preview:addToBasket` — добавляет/удаляет товар из корзины, закрывает модалку.
4. `basket:removeItem` — удаляет товар из корзины.
5. `card:open` — находит товар и устанавливает как выбранный.
6. `card:selected` — создает CardPreview и открывает модалку.
7. `basket:open` — открывает корзину в модалке.
8. `basket:submit` — открывает форму заказа.
9. `order.*:changed` — обновляют модель покупателя.
10. `order:submit` — открывает форму контактов.
11. `contacts.*:changed` — обновляют модель покупателя.
12. `buyer:changed` — обновляет обе формы валидацией.
13. `contacts:submit` — отправляет заказ, очищает корзину и показывает успех.
14. `modal:open / modal:close` — блокирует/разблокирует прокрутку.
15. `success:close` — закрывает модалку.

###### Шаблоны (Templates)

Шаблоны карточек товара
Карточка в каталоге	`#card-catalog`	Отображение товаров в галерее
Карточка в превью	`#card-preview`	Отображение товара в модальном окне
Карточка в корзине	`#card-basket`	Отображение товара в списке корзины

Шаблоны модальных окон
Корзина	`#basket`	Отображение списка товаров и итоговой суммы
Заказ	`#order`	Форма выбора оплаты и ввода адреса
Контакты	`#contacts`	Форма ввода email и телефона
Успех	`#success`	Сообщение об успешном оформлении заказа